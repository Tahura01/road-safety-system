server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Essential for Single Page Applications (React/Vite) to handle routing
        try_files $uri $uri/ /index.html;
    }
}
